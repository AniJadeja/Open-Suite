// Personal Access Token (PAT) for Zitadel self-hosted SSO login client
# Using Zitadel CLI
zitadel pat create --user-id <USER_ID>

# Or via the Zitadel Console UI
# 1. Navigate to Settings > Personal Access Tokens
# 2. Click "New"
# 3. Set expiration and scopes
# 4. Copy the generated token
